# Football Data Pipeline with Selenium

A complete ETL pipeline that scrapes football match data using Selenium, cleans it, and stores it in SQLite.

## Installation

```bash
# 1. Create virtual environment
python3 -m venv venv
source venv/bin/activate

# 2. Install requirements
pip install -r requirements.txt